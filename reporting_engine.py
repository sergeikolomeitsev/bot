# ============================================================
# REPORTING ENGINE v9.0 — AI PRIME TRADING BOT
# ------------------------------------------------------------
# Отвечает за:
# - daily report
# - weekly report
# НЕ:
# - формирует графики (это делает TelegramBot)
# - анализирует рынок
# - торгует
# ============================================================

class ReportingEngine:
    """
    Чистый модуль отчётности.
    Loop вызывает его в строго определённые моменты.
    """

    def __init__(self, telegram_bot):
        self.bot = telegram_bot

    # ------------------------------------------------------------
    # PUBLIC — DAILY REPORT
    # ------------------------------------------------------------
    def send_daily_report(self) -> None:
        msg = "📊 Daily Report v9.0 — выполнен."
        self.bot.send_message(msg)

    # ------------------------------------------------------------
    # PUBLIC — WEEKLY REPORT
    # ------------------------------------------------------------
    def send_weekly_report(self) -> None:
        msg = "📈 Weekly Report v9.0 — выполнен."
        self.bot.send_message(msg)
